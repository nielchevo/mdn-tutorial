extends layout

block content

    h1=title

    p this is page should contain all books list 

    ul
    each book in book_list
        li 
            a(href=book.url) #{book.title} (#{book.author.name})
    else
        li o data to be rendered!
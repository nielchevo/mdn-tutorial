extends layout

block content
  h1= title
    p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.


    h1 Dynamic content

    if error
      p Error getting dynamic content.

    p This book has the following record below :

    ul
      li Book Stored Count : !{data.book_count}
      li Book Copy Count : !{data.book_instance_count}
      li Book Copy Available : !{data.book_instance_available_count}
      li Book Copy is Loaned : !{data.book_instance_loaned_count}
      li Total count of Author : !{data.author_count}
      li Total count of Genre  : !{data.genre_count}